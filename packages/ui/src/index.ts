// Public barrel for @fitcrew/ui — shared design-system components (CLAUDE.md §4/§5).
// Consumed by /apps/web only. Components land alongside the screens that need
// them, starting Level 1.3 (login) and 2.1 (coach management UI).

export const UI_PACKAGE_NAME = '@fitcrew/ui';
