import { auth } from '@/auth'; import { mediaStorage } from '@/lib/media-storage'; import { cleanSettlementError, downloadPayslipForUser, prisma } from '@fitcrew/db'; import { NextResponse } from 'next/server';
export async function GET(request: Request, { params }: { params: { id: string } }) { const session = await auth(); const tenantId = new URL(request.url).searchParams.get('tenantId'); if (!session?.user?.id) return NextResponse.json({ error: 'Unauthorized.' }, { status: 401 }); if (!tenantId) return NextResponse.json({ error: 'Invalid tenant.' }, { status: 400 }); try { const result = await downloadPayslipForUser(prisma, tenantId, session.user.id, params.id, mediaStorage); return new Response(Uint8Array.from(result.bytes).buffer, { headers: { 'content-type': 'application/pdf', 'content-disposition': `inline; filename="${result.filename}"`, 'cache-control': 'private, no-store' } }); } catch (error) { return NextResponse.json({ error: cleanSettlementError(error) }, { status: 403 }); } }
