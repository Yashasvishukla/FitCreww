export default function HomePage() {
  return (
    <main style={{ padding: 'var(--space-4)' }}>
      <h1 style={{ fontSize: 'var(--font-size-xl)', fontWeight: 600 }}>FitCrew</h1>
      <p style={{ color: 'var(--color-text-secondary)' }}>
        Skeleton build — Level 1.1. Tenancy, identity, and role-scoped surfaces land next.
      </p>
    </main>
  );
}
