'use client';
import { useState } from 'react';
type Option = { id: string; label: string };
export function EnrollmentForm({ tenantId, coaches, organizations }: { tenantId: string; coaches: readonly Option[]; organizations: readonly Option[] }) { const [message, setMessage] = useState(''); const [pending, setPending] = useState(false); async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); setMessage(''); const data = new FormData(event.currentTarget); const response = await fetch('/api/clients', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ tenantId, name: data.get('name'), price: data.get('price'), coachPartyId: data.get('coachPartyId'), organizationId: data.get('organizationId') || null, schedule: { days: data.get('schedule') }, photoConsent: data.get('consent') === 'on', subscriptionDurationMonths: Number(data.get('duration')) }) }); const result = await response.json() as { error?: string }; setPending(false); setMessage(response.ok ? 'Client enrolled.' : (result.error ?? 'Enrollment failed.')); if (response.ok) event.currentTarget.reset(); } return <form className="auth-form" onSubmit={submit}><div className="form-grid"><label><span>Name</span><input name="name" required maxLength={200} /></label><label><span>Custom price (INR)</span><input name="price" type="number" min="0" step="0.01" required /></label><label><span>Coach</span><select name="coachPartyId" required defaultValue=""><option value="" disabled>Select a coach</option>{coaches.map((coach) => <option key={coach.id} value={coach.id}>{coach.label}</option>)}</select></label><label><span>Organization <small>(optional)</small></span><select name="organizationId" defaultValue=""><option value="">Direct client</option>{organizations.map((organization) => <option key={organization.id} value={organization.id}>{organization.label}</option>)}</select></label><label><span>Schedule</span><input name="schedule" placeholder="Mon, Wed, Fri" required /></label><label><span>Subscription duration</span><select name="duration" defaultValue="1">{[1, 3, 6, 12].map((value) => <option key={value} value={value}>{value} month{value === 1 ? '' : 's'}</option>)}</select></label></div><label className="checkbox-row"><input name="consent" type="checkbox" /> Photo consent granted</label><button className="primary-button" type="submit" disabled={pending || coaches.length === 0}>{pending ? 'Enrolling...' : coaches.length === 0 ? 'No coach available' : 'Enroll client'}</button>{message ? <p role="status" className={message === 'Client enrolled.' ? 'success-message' : 'form-error'}>{message}</p> : null}</form>; }
