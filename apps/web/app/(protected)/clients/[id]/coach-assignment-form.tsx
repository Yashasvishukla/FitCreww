'use client';
import { useState } from 'react';
export function CoachAssignmentForm({ tenantId, clientId, coaches, currentCoachId }: { tenantId: string; clientId: string; coaches: readonly { partyId: string; displayName: string }[]; currentCoachId: string | null }) {
  const [message, setMessage] = useState(''); const [pending, setPending] = useState(false);
  async function submit(event: React.FormEvent<HTMLFormElement>) { event.preventDefault(); setPending(true); setMessage(''); const data = new FormData(event.currentTarget); const response = await fetch('/api/clients', { method: 'PATCH', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ tenantId, clientId, coachPartyId: data.get('coachPartyId'), reason: data.get('reason') || undefined }) }); const result = await response.json() as { error?: string }; setPending(false); setMessage(response.ok ? 'Coach assignment updated.' : (result.error ?? 'Assignment failed.')); }
  return <form className="inline-form" onSubmit={submit}><label><span>Responsible coach</span><select name="coachPartyId" required defaultValue={currentCoachId ?? ''}><option value="" disabled>Select a coach</option>{coaches.map((coach) => <option key={coach.partyId} value={coach.partyId}>{coach.displayName}</option>)}</select></label><label><span>Reason <small>(optional)</small></span><input name="reason" maxLength={500} /></label><button className="secondary-button" type="submit" disabled={pending || coaches.length === 0}>{pending ? 'Saving...' : 'Save assignment'}</button>{message ? <p role="status" className="form-status">{message}</p> : null}</form>;
}
