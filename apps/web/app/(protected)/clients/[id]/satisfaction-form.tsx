'use client';
import { useState } from 'react';
export function SatisfactionForm({ tenantId, clientId }: { tenantId: string; clientId: string }) { const [message, setMessage] = useState(''); async function submit(e: React.FormEvent<HTMLFormElement>) { e.preventDefault(); const data = new FormData(e.currentTarget); const response = await fetch('/api/clients/satisfaction', { method: 'POST', headers: {'content-type':'application/json'}, body: JSON.stringify({ tenantId, clientId, score: Number(data.get('score')), comment: data.get('comment') }) }); setMessage(response.ok ? 'Satisfaction recorded.' : 'Could not record satisfaction.'); if (response.ok) e.currentTarget.reset(); } return <form className="inline-form" onSubmit={submit}><label><span>Score</span><select name="score" defaultValue="5"><option value="5">5 · Excellent</option><option value="4">4 · Good</option><option value="3">3 · Okay</option><option value="2">2 · Poor</option><option value="1">1 · Very poor</option></select></label><label><span>Comment</span><input name="comment" maxLength={2000} /></label><button className="secondary-button">Submit</button>{message ? <span role="status" className="muted">{message}</span> : null}</form>; }
